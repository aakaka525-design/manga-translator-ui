# Cloud Build upload filters.
# Start from .gitignore, then re-include runtime files that are ignored there.

.gcloudignore
.git

# CRITICAL: .env contains secrets — must NOT be uploaded to Cloud Build
# python-dotenv (load_dotenv(override=True)) would override Cloud Run env vars / Secret Manager
.env
.env.*

#!include:.gitignore

# Keep source upload small for Cloud Build.
frontend/node_modules/

# Runtime modules required by cloudrun_compute_main import path.
!manga_translator/utils/panel/lib/
!manga_translator/utils/panel/lib/**

# Runtime assets required by rendering/translation.
!dict/
!dict/**
!fonts/
!fonts/**
